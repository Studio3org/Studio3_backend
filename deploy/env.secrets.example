# Optional secrets for deploy.sh — add to deploy/config.env (never commit).
# create-infra.sh already writes DATABASE_URL / REDIS_URL / EC2_HOST.

# JWT_SECRET=
# SECRET_KEY=
# FRONTEND_URL=https://app.yourbrand.com

# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# S3_BUCKET=
# S3_PUBLIC_BASE_URL=
# SES_FROM_EMAIL=
# FIREBASE_SERVICE_ACCOUNT_JSON=
